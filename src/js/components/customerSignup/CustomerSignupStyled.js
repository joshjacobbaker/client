import styled from "styled-components"
import { MainContainer } from "../../globals/GlobalStyled"

const CustomerSignupStyled = styled(MainContainer)`
  display: grid;
  justify-content: center;
  align-items: center;
`

export default CustomerSignupStyled
